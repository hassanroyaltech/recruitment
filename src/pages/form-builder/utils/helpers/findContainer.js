// should find if id exists in array of obj. and returns Boolean
// export default function findContainer(id, items) {
//   if (id in items) {
//     return id;
//   }

//   return Object.keys(items).find((key) => items[key].includes(id));
// }
